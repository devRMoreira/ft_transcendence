services:
  db:
    image: postgres:18
    restart: always
    environment:
      POSTGRES_USER: cardgame
      POSTGRES_PASSWORD: cardgame_dev_password
      POSTGRES_DB: cardgame
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
